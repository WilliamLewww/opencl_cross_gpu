__kernel void squareKernel(__global float* input, __global float* output) {
  size_t idX = get_global_id(0);
  size_t idY = get_global_id(1);
  float id = idY * 512 + idX;

  float value = id * id;
  for (int x = 0; x < 50000; x++) {
    value += sqrt(id);
  }

  output[(int)id] = value;
}